# 复制为 .dev.vars 后填写；不要提交真实密钥。
LINUXDO_CLIENT_SECRET="replace-with-linux-do-client-secret"
SESSION_JWT_SECRET="replace-with-at-least-32-random-characters"

# 生产回调地址如果不是当前请求 origin，可显式指定并在 Linux DO 控制台登记：
# LINUXDO_REDIRECT_URI="https://example.com/api/v1/auth/callback"
